import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Insights" };

export default function InsightsPage() {
  return (
    <PagePlaceholder
      title="Insights"
      description="Confidence trends, weekly velocity against your goal, a coverage heatmap, your streak calendar, and DSA progress."
      note="Arriving in build block 16"
    />
  );
}
