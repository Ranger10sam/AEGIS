import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Log session" };

export default function LogPage() {
  return (
    <PagePlaceholder
      title="Log a session"
      description="Record what you studied — the type, the concept or pattern, your depth and confidence, your mood, and a few honest notes."
      note="Arriving in build block 8"
    />
  );
}
