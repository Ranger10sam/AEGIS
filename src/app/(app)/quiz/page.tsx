import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Quiz" };

export default function QuizPage() {
  return (
    <PagePlaceholder
      title="Quiz"
      description="Concept-tied flashcards. Reveal the answer, rate yourself honestly, and let low-accuracy questions resurface more often."
      note="Arriving in build block 12"
    />
  );
}
