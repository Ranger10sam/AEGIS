import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Theme, session sounds, notifications, your daily goal, prep duration, and email — all in one calm place."
      note="Arriving in build block 7"
    />
  );
}
