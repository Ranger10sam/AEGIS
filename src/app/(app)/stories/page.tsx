import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Stories" };

export default function StoriesPage() {
  return (
    <PagePlaceholder
      title="Story bank"
      description="STAR stories grounded in your own TaskFlow work, linked to the concepts they demonstrate, with flashcard-style recall."
      note="Arriving in build block 14"
    />
  );
}
