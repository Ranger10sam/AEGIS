import type { Difficulty, QuizCategory } from "../lib/database.types";

export interface QuizQuestionSeed {
  category: QuizCategory;
  /** Spring concept id or DSA pattern id; null for behavioral. */
  conceptId: string | null;
  difficulty: Difficulty;
  question: string;
  answer: string;
}

/**
 * Quiz bank (CLAUDE.md §9). 124 questions, generated and
 * accuracy-verified by the quiz workflow. Seeded into quiz_questions per user.
 */
export const QUIZ_SEED: QuizQuestionSeed[] = [
  {
    category: "spring",
    conceptId: "spring-ioc",
    difficulty: "easy",
    question: "What is the Spring IoC container, and what does it actually manage?",
    answer: "The IoC (Inversion of Control) container is the part of Spring that creates, configures, wires together, and manages the lifecycle of your application objects, called beans. Instead of your code calling new and managing dependencies itself, you hand that control to the container, which injects collaborators for you. This is why in TaskFlow you never instantiate TaskService or TaskRepository manually; the container builds them and supplies them where needed.",
  },
  {
    category: "spring",
    conceptId: "spring-ioc",
    difficulty: "medium",
    question: "What is the difference between BeanFactory and ApplicationContext?",
    answer: "BeanFactory is the basic container interface providing core dependency injection and lazy, on-demand bean instantiation. ApplicationContext is a superset that adds enterprise features: eager singleton instantiation at startup, event publishing, internationalization, convenient resource access, and automatic detection and registration of BeanPostProcessors and BeanFactoryPostProcessors. In practice you always use ApplicationContext (Spring Boot wires up one for you), because the extra features and fail-fast startup behavior are what you want in a real app like TaskFlow.",
  },
  {
    category: "spring",
    conceptId: "spring-ioc",
    difficulty: "hard",
    question: "When does the ApplicationContext instantiate singleton beans, and why does that matter for catching configuration errors?",
    answer: "By default the ApplicationContext eagerly instantiates all non-lazy singleton beans during startup (the refresh phase), not on first use. This matters because misconfiguration, missing dependencies, or unresolvable circular references surface immediately when the app boots rather than at runtime when a user hits an endpoint. So in TaskFlow, if TaskService had an unsatisfiable dependency, the app would fail to start cleanly instead of throwing later during a request. You can opt into lazy initialization with @Lazy or spring.main.lazy-initialization, but you trade away that fail-fast safety.",
  },
  {
    category: "spring",
    conceptId: "spring-di-types",
    difficulty: "easy",
    question: "What are the three types of dependency injection Spring supports?",
    answer: "Constructor injection, where dependencies are passed through the constructor; setter injection, where Spring calls setter methods after construction; and field injection, where Spring sets fields directly via reflection, typically with @Autowired on the field. Constructor injection is the recommended default for required dependencies because it makes them explicit and guarantees the object is fully initialized once created.",
  },
  {
    category: "spring",
    conceptId: "spring-di-types",
    difficulty: "medium",
    question: "Why is constructor injection preferred over field injection?",
    answer: "Constructor injection makes dependencies explicit and lets you mark fields final, giving you immutable, fully-initialized objects. It allows unit testing without a Spring container because you just pass mocks to the constructor, and it fails fast at startup if a dependency is missing rather than throwing a NullPointerException later. It also surfaces design smells: if a constructor takes too many arguments or a circular dependency exists, the problem is visible instead of hidden. Field injection hides all of this and cannot set final fields.",
  },
  {
    category: "spring",
    conceptId: "spring-di-types",
    difficulty: "hard",
    question: "If a Spring bean has a single constructor, do you still need @Autowired on it, and how does this relate to TaskFlow's TaskService?",
    answer: "No. Since Spring 4.3, if a bean has exactly one constructor, the container automatically uses it for injection and @Autowired is optional. So in TaskFlow, TaskService can declare a constructor taking TaskRepository as a final field with no annotation, and Spring will inject it. You only need @Autowired (or @Qualifier) when there are multiple constructors and you must tell Spring which one to use, or to disambiguate which to treat as the injection point.",
  },
  {
    category: "spring",
    conceptId: "spring-bean-lifecycle",
    difficulty: "easy",
    question: "What is the default scope of a Spring bean, and what does it mean?",
    answer: "The default scope is singleton, meaning the container creates exactly one shared instance per ApplicationContext and returns that same instance everywhere it is injected. This is why TaskFlow's TaskService and TaskRepository are single shared objects across all requests. Because singletons are shared, they should be stateless and thread-safe, since many concurrent requests use the same instance.",
  },
  {
    category: "spring",
    conceptId: "spring-bean-lifecycle",
    difficulty: "medium",
    question: "What are the main ways to run initialization and cleanup logic in a bean's lifecycle?",
    answer: "For initialization you can implement InitializingBean's afterPropertiesSet, annotate a method with @PostConstruct, or declare an initMethod on @Bean. For cleanup you can implement DisposableBean's destroy, annotate with @PreDestroy, or declare a destroyMethod on @Bean. The annotation-based @PostConstruct and @PreDestroy (from the Jakarta annotations in Spring Boot 3) are usually preferred because they keep your bean decoupled from Spring interfaces. Init runs after dependencies are injected; destroy runs at container shutdown, and only reliably for singletons.",
  },
  {
    category: "spring",
    conceptId: "spring-bean-lifecycle",
    difficulty: "hard",
    question: "Why are prototype-scoped beans not given a destroy callback by the container, and what is the implication?",
    answer: "For prototype beans, Spring instantiates, configures, and runs initialization, then hands the bean to the caller and forgets about it; it does not retain a reference. Because the container no longer tracks the instance, it cannot invoke @PreDestroy or destroy callbacks at shutdown, so cleanup becomes the client's responsibility. The implication is that if a prototype bean holds an expensive or closable resource, you must release it yourself, often via a custom BeanPostProcessor or explicit client code, rather than relying on the container.",
  },
  {
    category: "spring",
    conceptId: "spring-annotations-core",
    difficulty: "easy",
    question: "What is the relationship between @Component and the @Service, @Repository, and @Controller annotations?",
    answer: "@Service, @Repository, and @Controller are all specializations of @Component; they are meta-annotated with it. All four mark a class as a Spring-managed bean to be detected by component scanning. The specialized ones add semantic meaning about the layer (service logic, persistence, web), which both documents intent and, in some cases, enables extra behavior. In TaskFlow, TaskService is @Service, TaskRepository is an interface backed by a generated repository proxy, and TaskController is @RestController.",
  },
  {
    category: "spring",
    conceptId: "spring-annotations-core",
    difficulty: "medium",
    question: "What concrete benefit does @Repository provide beyond marking a class as a bean?",
    answer: "Beyond being a stereotype that makes the class a scanned bean, @Repository enables Spring's persistence exception translation. A PersistenceExceptionTranslationPostProcessor wraps the bean so vendor-specific exceptions, such as JPA PersistenceExceptions or JDBC SQLExceptions, are translated into Spring's consistent DataAccessException hierarchy. This decouples your service code from the underlying persistence technology, so in TaskFlow your TaskService can handle a uniform unchecked exception type rather than database-driver-specific ones.",
  },
  {
    category: "spring",
    conceptId: "spring-annotations-core",
    difficulty: "hard",
    question: "What is the difference between @Controller and @RestController, and which does TaskFlow's TaskController use?",
    answer: "@Controller marks a web controller whose handler methods, by default, return view names resolved by a ViewResolver, used in server-side rendered MVC. @RestController is a convenience annotation that combines @Controller and @ResponseBody, so every handler's return value is serialized directly into the response body, typically as JSON. A REST API like TaskFlow's TaskController uses @RestController so that returning a Task or a list of tasks produces a JSON payload instead of being interpreted as a view name.",
  },
  {
    category: "spring",
    conceptId: "spring-auto-config",
    difficulty: "easy",
    question: "What three annotations does @SpringBootApplication combine?",
    answer: "@SpringBootApplication is a convenience meta-annotation that bundles @SpringBootConfiguration (a specialized @Configuration marking the class as a source of bean definitions), @EnableAutoConfiguration (which triggers Spring Boot's auto-configuration), and @ComponentScan (which scans the package of the annotated class and its subpackages for beans). Placing it on TaskFlow's Application class is why all the @Service, @RestController, and repository beans get discovered automatically.",
  },
  {
    category: "spring",
    conceptId: "spring-auto-config",
    difficulty: "medium",
    question: "How does Spring Boot auto-configuration decide which configurations to apply?",
    answer: "In Spring Boot 3, auto-configuration classes are listed in META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports on the classpath, and each is guarded by @Conditional annotations such as @ConditionalOnClass, @ConditionalOnMissingBean, and @ConditionalOnProperty. At startup Spring Boot evaluates these conditions against what is actually on the classpath and already defined, so for example finding a JDBC driver and datasource settings triggers DataSource auto-configuration. Critically, @ConditionalOnMissingBean means your own beans take precedence and the defaults back off, so defining a custom bean in TaskFlow overrides the auto-configured one.",
  },
  {
    category: "spring",
    conceptId: "spring-auto-config",
    difficulty: "hard",
    question: "If Spring Boot's security auto-configuration secures every endpoint by default, how do you customize it for an app like TaskFlow that uses JWT, and why does that work?",
    answer: "You define your own SecurityFilterChain bean in a @Configuration class. Spring Security's auto-configuration uses @ConditionalOnMissingBean for the default filter chain, so as soon as you supply your own SecurityFilterChain it backs off and your configuration takes over. This lets TaskFlow set the API to stateless, permit the auth endpoints, require authentication elsewhere, and insert its custom JWT filter via addFilterBefore, replacing the default HTTP Basic and form login that auto-configuration would otherwise impose.",
  },
  {
    category: "spring",
    conceptId: "spring-properties",
    difficulty: "easy",
    question: "What is the difference between using @Value and @ConfigurationProperties to read configuration?",
    answer: "@Value injects a single property by key, such as @Value(\"${jwt.expiration}\"), and is convenient for one-off values. @ConfigurationProperties binds a whole group of related properties under a common prefix to the fields of a typed POJO, giving you type-safe, structured, validatable configuration. For TaskFlow's JWT settings (secret, expiration, issuer), a @ConfigurationProperties(prefix = \"jwt\") class is cleaner and easier to test than scattering several @Value annotations.",
  },
  {
    category: "spring",
    conceptId: "spring-properties",
    difficulty: "medium",
    question: "What advantages does @ConfigurationProperties have over @Value for grouped settings?",
    answer: "@ConfigurationProperties offers type-safe binding of a group of properties into a single object, supports relaxed binding (so jwt.access-token-expiration maps to accessTokenExpiration), and works with Jakarta Bean Validation via @Validated to fail fast on bad config at startup. It also supports nested objects and collections, and keeps related configuration cohesive in one class rather than spread across many @Value strings. The main things @Value still wins at are SpEL expressions and quick single-value injection.",
  },
  {
    category: "spring",
    conceptId: "spring-properties",
    difficulty: "hard",
    question: "If the same property is defined in application.yml and also in an environment variable, which wins, and how does Spring Boot resolve it?",
    answer: "The environment variable wins. Spring Boot merges all sources into a single Environment with a well-defined precedence order, and OS environment variables sit higher than application.yml or application.properties files. The order, from higher to lower, includes command-line arguments, then SPRING_APPLICATION_JSON, then OS environment variables, then profile-specific files, then the base application.yml, and finally @PropertySource and defaults. This layering, combined with relaxed binding (so JWT_SECRET maps to jwt.secret), is exactly how TaskFlow can keep safe defaults in application.yml while overriding the JWT secret with a real environment variable in production.",
  },
  {
    category: "spring",
    conceptId: "spring-rest-basics",
    difficulty: "easy",
    question: "What is the difference between @Controller and @RestController in Spring?",
    answer: "@RestController is a convenience annotation that combines @Controller and @ResponseBody, so every handler method's return value is serialized directly into the HTTP response body rather than being resolved as a view name. In a REST API like TaskFlow, TaskController uses @RestController so returning a Task object automatically gets serialized to JSON. With plain @Controller you would have to annotate each method with @ResponseBody to get the same behavior.",
  },
  {
    category: "spring",
    conceptId: "spring-rest-basics",
    difficulty: "medium",
    question: "When do you use @PathVariable versus @RequestParam, and how would each look in a TaskFlow endpoint?",
    answer: "@PathVariable binds a value embedded in the URI path, used for identifying a specific resource, for example GET /tasks/{id} where @PathVariable Long id extracts the task identifier. @RequestParam binds a query-string parameter, used for filtering, sorting, or optional inputs, for example GET /tasks?status=OPEN where @RequestParam String status reads the filter. The rule of thumb is path variables identify the resource while request params modify or filter the query.",
  },
  {
    category: "spring",
    conceptId: "spring-rest-basics",
    difficulty: "medium",
    question: "How does Spring convert the JSON request body into a Java object when you use @RequestBody, and what is required for it to work?",
    answer: "Spring uses an HttpMessageConverter, by default the Jackson-based MappingJackson2HttpMessageConverter, to deserialize the request body into the target type based on the Content-Type header. When TaskController declares @RequestBody CreateTaskRequest dto, Jackson maps the JSON fields onto the DTO's properties using getters/setters or the constructor. This requires Jackson on the classpath (included via spring-boot-starter-web) and the incoming request to send Content-Type application/json.",
  },
  {
    category: "spring",
    conceptId: "spring-response-entity",
    difficulty: "easy",
    question: "What does ResponseEntity give you that simply returning a domain object does not?",
    answer: "ResponseEntity lets you control the full HTTP response, including the status code, response headers, and the body, rather than only the body. Returning a Task object directly always yields 200 OK with no custom headers, whereas returning ResponseEntity lets a TaskController create endpoint respond with 201 Created plus a Location header pointing at the new resource. It gives explicit, per-request control over the response when the default 200 is not correct.",
  },
  {
    category: "spring",
    conceptId: "spring-response-entity",
    difficulty: "medium",
    question: "Which HTTP status codes should a REST API like TaskFlow return for a successful create, a successful delete with no body, and a request for a task that does not exist?",
    answer: "A successful create should return 201 Created, ideally with a Location header for the new resource, signaling a new entity was persisted. A successful delete that returns no payload should return 204 No Content, indicating success with an intentionally empty body. A request for a non-existent task should return 404 Not Found so the client can distinguish a missing resource from a server error or an empty-but-valid result.",
  },
  {
    category: "spring",
    conceptId: "spring-response-entity",
    difficulty: "hard",
    question: "What is the practical difference between ResponseEntity<Void>, ResponseEntity<?> and ResponseEntity.noContent(), and when would each appear in TaskFlow?",
    answer: "ResponseEntity<Void> declares a response with no body type, used for endpoints like DELETE /tasks/{id} that return 204 with nothing in the body. ResponseEntity<?> uses a wildcard so a single method can return different body types on different branches, for example a Task on success or an error DTO otherwise, though that is usually better handled by exception handlers. ResponseEntity.noContent() is a builder shortcut that produces a 204 No Content response and is the idiomatic way to express a successful delete in TaskService-backed controllers.",
  },
  {
    category: "spring",
    conceptId: "spring-exception-handling",
    difficulty: "medium",
    question: "What is the role of @ControllerAdvice together with @ExceptionHandler, and why is it better than try/catch in every controller method?",
    answer: "@ControllerAdvice (or @RestControllerAdvice) defines a class whose @ExceptionHandler methods handle exceptions thrown by any controller across the application, centralizing error handling in one place. In TaskFlow a GlobalExceptionHandler can map TaskNotFoundException to 404 and validation failures to 400 consistently, instead of repeating try/catch blocks in TaskController. This keeps controllers focused on the happy path and guarantees uniform error responses across all endpoints.",
  },
  {
    category: "spring",
    conceptId: "spring-exception-handling",
    difficulty: "hard",
    question: "What is ProblemDetail in Spring Boot 3, and how does it improve API error responses?",
    answer: "ProblemDetail is Spring's implementation of RFC 7807, a standardized format served as application/problem+json for representing errors with fields like type, title, status, detail, and instance. Returning ProblemDetail from a GlobalExceptionHandler gives clients a predictable, machine-readable error contract instead of an ad-hoc JSON shape that varies per endpoint. You can also extend it with custom properties, and in Boot 3 the built-in ResponseEntityExceptionHandler already produces ProblemDetail for common framework exceptions once ProblemDetails support is enabled.",
  },
  {
    category: "spring",
    conceptId: "spring-exception-handling",
    difficulty: "easy",
    question: "What is the difference between @ControllerAdvice and @RestControllerAdvice?",
    answer: "@RestControllerAdvice is a combination of @ControllerAdvice and @ResponseBody, so the values returned by its @ExceptionHandler methods are serialized directly into the response body as JSON. Plain @ControllerAdvice would treat a String return as a view name unless each handler is also annotated with @ResponseBody. For a JSON REST API like TaskFlow you use @RestControllerAdvice so error objects like ProblemDetail are written straight to the body.",
  },
  {
    category: "spring",
    conceptId: "spring-validation",
    difficulty: "easy",
    question: "What does @Valid do on a controller method parameter, and which annotations does it trigger?",
    answer: "@Valid is the Jakarta Bean Validation annotation that tells Spring to validate the annotated argument before the handler executes, checking constraint annotations such as @NotNull, @NotBlank, @Size, @Email, and @Min/@Max on the object's fields. In TaskFlow, putting @Valid @RequestBody CreateTaskRequest on the create endpoint ensures fields like title are non-blank before any service logic runs. If a constraint fails on a request body, Spring throws a MethodArgumentNotValidException instead of invoking the method. Spring's own @Validated is similar but additionally supports validation groups.",
  },
  {
    category: "spring",
    conceptId: "spring-validation",
    difficulty: "medium",
    question: "What happens when validation fails on a @Valid @RequestBody, and how do you turn that into a clean 400 response?",
    answer: "When validation fails on a @RequestBody, Spring throws MethodArgumentNotValidException, which by default produces a 400 Bad Request. To return a clean, structured response you handle that exception in a @RestControllerAdvice, read the BindingResult from it, and map each FieldError into a tidy payload of field-to-message pairs (or a ProblemDetail). This gives the client precise feedback like 'title must not be blank' rather than a raw stack trace or generic error.",
  },
  {
    category: "spring",
    conceptId: "spring-validation",
    difficulty: "hard",
    question: "What is BindingResult, and why must its parameter position matter when you use it directly in a controller method?",
    answer: "BindingResult holds the outcome of validation and data binding, including any field and global errors, letting you inspect failures programmatically inside the handler instead of having Spring throw an exception. It must be declared immediately after the @Valid object it refers to, because Spring pairs each validated argument with the BindingResult that directly follows it. When a BindingResult immediately follows the validated body, Spring does not throw MethodArgumentNotValidException and instead passes control to your method, so you become responsible for checking hasErrors() and responding accordingly.",
  },
  {
    category: "spring",
    conceptId: "spring-filter-interceptor",
    difficulty: "medium",
    question: "What is the difference between a servlet Filter (OncePerRequestFilter) and a Spring HandlerInterceptor in terms of where they sit in request processing?",
    answer: "A Filter operates at the servlet container level, wrapping the entire request before it ever reaches Spring's DispatcherServlet, so it can see and modify the raw request and response and runs even for non-MVC requests. A HandlerInterceptor operates inside Spring MVC, after the DispatcherServlet has resolved which handler will run, giving it access to the matched handler method via preHandle, postHandle, and afterCompletion. In short, filters are broader and lower-level while interceptors are MVC-aware and handler-specific.",
  },
  {
    category: "spring",
    conceptId: "spring-filter-interceptor",
    difficulty: "hard",
    question: "Why is OncePerRequestFilter the right base class for a JWT authentication filter like TaskFlow's JwtFilter?",
    answer: "OncePerRequestFilter guarantees its doFilterInternal method runs exactly once per request, even when the request is dispatched again internally (such as forwards, includes, or async dispatches) which could otherwise cause a plain Filter to execute multiple times. For a JWT filter this matters because re-parsing the token and re-populating the SecurityContext on every internal dispatch would be wasteful and could cause inconsistent authentication state. It also fits Spring Security's filter chain, where authentication must be established early, before authorization and the controller run.",
  },
  {
    category: "spring",
    conceptId: "spring-filter-interceptor",
    difficulty: "easy",
    question: "Which HandlerInterceptor method runs before the controller, and what can you do in it?",
    answer: "The preHandle method runs after the handler is selected but before the controller method executes, and it returns a boolean. Returning false stops further processing so the request never reaches the controller, which is useful for cross-cutting checks like simple authorization, logging, or rate limiting. Returning true lets the request continue to the handler, and you can also inspect the matched handler and modify the request or response before it proceeds.",
  },
  {
    category: "spring",
    conceptId: "spring-data-jpa",
    difficulty: "easy",
    question: "What does the @Entity annotation do, and what is the minimum requirement for a class to be a valid JPA entity?",
    answer: "@Entity marks a class as a persistent domain object that JPA maps to a database table, so an entity like Task becomes a row-mapped type managed by the persistence context. At minimum the class needs a public or protected no-argument constructor and a primary key field annotated with @Id; without @Id the provider rejects it at startup because it cannot identify rows uniquely. In Spring Boot 3.x the annotation comes from jakarta.persistence, not javax.persistence.",
  },
  {
    category: "spring",
    conceptId: "spring-data-jpa",
    difficulty: "medium",
    question: "How does Spring Data JPA provide an implementation for an interface like TaskRepository extends JpaRepository<Task, Long> without you writing any code?",
    answer: "At startup Spring Data scans for repository interfaces and creates a dynamic proxy backed by SimpleJpaRepository, so you get CRUD methods like save, findById, and delete for free. For derived methods such as findByStatus, it parses the method name into a JPQL query when the repository is bootstrapped, which is why a typo in a property name fails fast at startup rather than later at runtime.",
  },
  {
    category: "spring",
    conceptId: "spring-data-jpa",
    difficulty: "medium",
    question: "What is the difference between save and saveAndFlush in a Spring Data JPA repository, and why does it matter?",
    answer: "save registers the entity with the persistence context but typically defers the actual SQL until the context is flushed, usually at transaction commit, so changes can be batched for efficiency. saveAndFlush forces the SQL to run immediately, which is useful when you need a database-generated value or constraint check to surface at that exact point, or when a subsequent native query in the same transaction must see the change. Note that with IDENTITY generation the insert happens at save time anyway because the ID is assigned by the database.",
  },
  {
    category: "spring",
    conceptId: "spring-data-jpa",
    difficulty: "hard",
    question: "What is the difference between EntityManager.persist and EntityManager.merge, and which one does Spring Data's save use?",
    answer: "persist makes a brand-new transient instance managed and is intended for entities without an existing identity, throwing EntityExistsException if the entity is already persistent. merge copies the state of a detached or new instance into a managed copy and returns that managed copy, so the argument you passed in is not the tracked entity. Spring Data's SimpleJpaRepository.save checks whether the entity is new, calling persist for new entities and merge otherwise, which is why you should always use the returned value from save.",
  },
  {
    category: "spring",
    conceptId: "spring-transactions",
    difficulty: "easy",
    question: "By default, on what kind of exception does a @Transactional method roll back, and what surprises developers about this?",
    answer: "By default Spring rolls back only on unchecked exceptions, meaning RuntimeException and Error, and commits normally if a checked exception propagates out. This surprises people because throwing a checked exception like a custom TaskNotFoundException that does not extend RuntimeException will still commit the transaction unless you add rollbackFor to the annotation.",
  },
  {
    category: "spring",
    conceptId: "spring-transactions",
    difficulty: "medium",
    question: "In TaskService, calling one @Transactional method from another method in the same class does not start a new transaction or apply the called method's settings. Why?",
    answer: "Spring's declarative transactions work through an AOP proxy, and a self-invocation goes directly through the target object's this reference, bypassing the proxy entirely. Because the transaction interceptor never runs for the internal call, the propagation and isolation settings on the called method are ignored and you stay in whatever transactional context the caller had. The fix is to move the inner method into a separate bean, or self-inject the proxy and call through it.",
  },
  {
    category: "spring",
    conceptId: "spring-transactions",
    difficulty: "hard",
    question: "What is the difference between REQUIRED and REQUIRES_NEW propagation, and when would you reach for REQUIRES_NEW in a service like TaskService?",
    answer: "REQUIRED, the default, joins an existing transaction or creates one if none exists, so everything commits or rolls back together. REQUIRES_NEW suspends any current transaction and runs in an independent one that commits or rolls back on its own. You would use REQUIRES_NEW for something like writing an audit log of a task change that must persist even if the surrounding business operation later rolls back, though be aware both transactions hold connections simultaneously, which can exhaust the pool under load.",
  },
  {
    category: "spring",
    conceptId: "spring-transactions",
    difficulty: "hard",
    question: "What problem does isolation level READ_COMMITTED prevent that READ_UNCOMMITTED does not, and what does it still allow?",
    answer: "READ_COMMITTED prevents dirty reads, so a transaction never sees another transaction's uncommitted changes, whereas READ_UNCOMMITTED permits reading data that may later be rolled back. READ_COMMITTED still allows non-repeatable reads and phantom reads, meaning re-running the same query within one transaction can return different values or rows because other transactions committed in between. Preventing those requires REPEATABLE_READ or SERIALIZABLE.",
  },
  {
    category: "spring",
    conceptId: "spring-jpql",
    difficulty: "easy",
    question: "What is the fundamental difference between a JPQL query and a native query in the @Query annotation?",
    answer: "JPQL operates over your entity model and its fields, for example select t from Task t where t.status = :status, and the provider translates it into database-specific SQL. A native query, enabled with nativeQuery = true, is raw SQL written against actual table and column names. JPQL keeps you portable across databases, while native queries trade portability for access to vendor-specific features.",
  },
  {
    category: "spring",
    conceptId: "spring-jpql",
    difficulty: "medium",
    question: "In a @Query on TaskRepository, what is the difference between positional parameters and named parameters, and which is preferred?",
    answer: "Positional parameters use ?1, ?2 bound by index, while named parameters use :status bound by name and matched to a method argument via @Param. Named parameters are preferred because they are self-documenting and resilient to reordering or adding parameters, which reduces bugs in queries with several arguments. They also read far more clearly in a real repository method.",
  },
  {
    category: "spring",
    conceptId: "spring-jpql",
    difficulty: "medium",
    question: "Why must you add @Modifying to a @Query that performs an UPDATE or DELETE, and what side effect should you be aware of?",
    answer: "@Modifying tells Spring Data the query is a DML statement returning an affected-row count rather than a result set, so it calls executeUpdate instead of getResultList. The key side effect is that such bulk updates bypass the persistence context, so entities already loaded in memory become stale. You often pair it with clearAutomatically or flushAutomatically on @Modifying, and the method must run inside a transaction.",
  },
  {
    category: "spring",
    conceptId: "spring-jpql",
    difficulty: "hard",
    question: "When mapping a native query result to a custom shape rather than a full entity in Spring Data JPA, what are your options and their trade-offs?",
    answer: "You can map to an interface-based projection, where Spring proxies an interface whose getters match the selected column aliases, which is lightweight and type-safe. Alternatively you can use @SqlResultSetMapping with @NamedNativeQuery for fine-grained control over entity, scalar, and constructor mappings. Interface projections are simplest for read-only views, while result-set mappings are more verbose but handle complex cases like entity-plus-scalar results; note that JPQL constructor expressions are not available for native queries.",
  },
  {
    category: "spring",
    conceptId: "spring-n-plus-one",
    difficulty: "easy",
    question: "What is the N+1 select problem in JPA, and how might it show up when loading tasks in TaskFlow?",
    answer: "The N+1 problem is when one query loads N parent entities and then a separate query fires for each parent to load a lazy association, producing N+1 total queries. In TaskFlow, calling findAll on tasks and then accessing each task's lazily-loaded comments or assignee in a loop triggers one extra query per task. It is a common cause of slow endpoints that looks fine in code but hammers the database.",
  },
  {
    category: "spring",
    conceptId: "spring-n-plus-one",
    difficulty: "medium",
    question: "How does @EntityGraph solve the N+1 problem, and how is it different from changing the association to FetchType.EAGER?",
    answer: "@EntityGraph on a repository method tells JPA to fetch the named associations for that specific query, typically via a join, so the related data loads up front without extra round trips. Unlike marking the association FetchType.EAGER, which forces eager loading everywhere and degrades every query that touches the entity, an entity graph is applied per query. That keeps the default lazy and only pays the join cost where you actually need the data.",
  },
  {
    category: "spring",
    conceptId: "spring-n-plus-one",
    difficulty: "medium",
    question: "Why is FetchType.LAZY the default for @OneToMany and @ManyToMany, while @ManyToOne and @OneToOne default to EAGER?",
    answer: "Collection associations default to LAZY because eagerly loading potentially large collections on every fetch would be expensive and often wasteful. Single-valued associations like @ManyToOne default to EAGER because loading one related row is usually cheap and frequently needed. In practice teams often override @ManyToOne to LAZY to avoid unintended eager joins and then fetch explicitly with a join fetch or entity graph when required.",
  },
  {
    category: "spring",
    conceptId: "spring-n-plus-one",
    difficulty: "hard",
    question: "Using JOIN FETCH in JPQL to avoid N+1 can cause a MultipleBagFetchException or duplicate rows. Why, and how do you handle it?",
    answer: "Join-fetching a single collection produces duplicate parent rows in the result set, so you add distinct or rely on the provider to de-duplicate in memory. Fetching two List-based (bag) collections at once throws MultipleBagFetchException because the SQL Cartesian product cannot be unambiguously de-duplicated into independent lists. The usual remedies are to use Set instead of List for the collections, fetch one collection per query, or rely on batch fetching via hibernate.default_batch_fetch_size.",
  },
  {
    category: "spring",
    conceptId: "spring-security-basics",
    difficulty: "easy",
    question: "What is the difference between authentication and authorization in Spring Security?",
    answer: "Authentication answers 'who are you?' by verifying credentials and establishing identity, while authorization answers 'what are you allowed to do?' by checking that the authenticated principal has the required roles or authorities for a resource. In Spring Security authentication populates an Authentication object in the SecurityContext, and authorization decisions (via authorizeHttpRequests or @PreAuthorize) are then made against the authorities on that principal. The order matters: you cannot authorize a request until you know who is making it.",
  },
  {
    category: "spring",
    conceptId: "spring-security-basics",
    difficulty: "medium",
    question: "In Spring Security 6, how do you configure HTTP security, and what does a typical SecurityFilterChain bean for an app like TaskFlow look like?",
    answer: "In Spring Security 6 you expose a SecurityFilterChain bean from an HttpSecurity object instead of extending the now-removed WebSecurityConfigurerAdapter. For a JWT-based API like TaskFlow you typically disable CSRF (since it's stateless), set the session creation policy to STATELESS, permit the login/register endpoints with authorizeHttpRequests, require authentication for everything else with anyRequest().authenticated(), and register your custom JWT filter before UsernamePasswordAuthenticationFilter via addFilterBefore. Returning a bean lets you define multiple filter chains and makes the configuration explicit and testable.",
  },
  {
    category: "spring",
    conceptId: "spring-security-basics",
    difficulty: "medium",
    question: "What is the SecurityContext and SecurityContextHolder, and how does TaskService get the current user from it?",
    answer: "The SecurityContext holds the Authentication object for the current principal, and SecurityContextHolder is the thread-local accessor that stores it per request. After a request is authenticated, code anywhere downstream can call SecurityContextHolder.getContext().getAuthentication() to retrieve the principal, authorities, and details. In TaskFlow, the JWT filter populates this context once per request, so TaskService can read the current username to scope tasks to their owner without passing the user through every method signature. Because the default strategy is thread-local, Spring's SecurityContextHolderFilter clears it at the end of the request so identity does not leak between pooled threads.",
  },
  {
    category: "spring",
    conceptId: "spring-security-basics",
    difficulty: "hard",
    question: "Why is the order of security filters in the SecurityFilterChain important, and what can go wrong if a custom filter is placed incorrectly?",
    answer: "Spring Security is a chain of servlet filters that run in a defined order, so a filter's position determines what state exists when it runs. A JWT authentication filter must run before the authorization filter (AuthorizationFilter, which replaced FilterSecurityInterceptor in Spring Security 6) so the SecurityContext is populated before access decisions are made; if it runs after, every request appears anonymous and gets rejected. Placing it on the wrong side of UsernamePasswordAuthenticationFilter can also let the default form-login filter run first or leave the context unset when authorization evaluates. The general rule is authentication filters early, authorization last, and use addFilterBefore/addFilterAfter relative to a known filter rather than guessing absolute positions.",
  },
  {
    category: "spring",
    conceptId: "spring-jwt",
    difficulty: "easy",
    question: "What are the three parts of a JWT, and which part makes it tamper-evident?",
    answer: "A JWT has three Base64Url-encoded parts separated by dots: the header (algorithm and token type), the payload (claims such as subject, issued-at, and expiry), and the signature. The signature is computed over the encoded header and payload using a secret or private key, so it is what makes the token tamper-evident: any change to the header or payload invalidates the signature. Note that the payload is only encoded, not encrypted, so you should never put secrets in JWT claims.",
  },
  {
    category: "spring",
    conceptId: "spring-jwt",
    difficulty: "medium",
    question: "Why does a JWT authentication filter typically extend OncePerRequestFilter, and what does it do on each request in an app like TaskFlow?",
    answer: "It extends OncePerRequestFilter to guarantee the filter logic executes exactly once per request, even when the request is dispatched internally (for example a forward or error dispatch), which prevents double validation. On each request the TaskFlow filter reads the Authorization header, strips the Bearer prefix, validates the token's signature and expiry, loads the user details for the subject, and if valid builds a UsernamePasswordAuthenticationToken and sets it on the SecurityContext. If the header is missing or the token is invalid it simply continues the chain without authenticating, letting the downstream authorization rules reject the request.",
  },
  {
    category: "spring",
    conceptId: "spring-jwt",
    difficulty: "medium",
    question: "Why is JWT-based authentication considered stateless, and what configuration enforces that in Spring Security?",
    answer: "It is stateless because the server stores no session for the client; all the identity and claims needed to authenticate the request are carried inside the token itself and verified by signature on each call. This lets any instance behind a load balancer serve any request without sticky sessions or shared session storage. In Spring Security you enforce it by setting the session creation policy to SessionCreationPolicy.STATELESS, which stops Spring from creating or using an HttpSession to store the SecurityContext between requests.",
  },
  {
    category: "spring",
    conceptId: "spring-jwt",
    difficulty: "hard",
    question: "What is the purpose of a refresh token alongside an access token, and how does that flow improve security?",
    answer: "The access token is short-lived and sent on every API call, while the longer-lived refresh token is stored more securely and used only to obtain a new access token when the current one expires. This limits the blast radius: if a short-lived access token leaks it is only valid for minutes, and you avoid forcing the user to re-enter credentials frequently. Because stateless JWTs cannot be revoked before they expire, the refresh token is often persisted server-side so it can be invalidated on logout or compromise, giving you a revocation point that pure access tokens lack.",
  },
  {
    category: "spring",
    conceptId: "spring-dto-pattern",
    difficulty: "easy",
    question: "What is the difference between a DTO and a JPA entity, and why separate them?",
    answer: "An entity (like Task) is mapped to a database table and managed by the persistence context, while a DTO is a plain object shaped for transferring data across a boundary such as an API request or response. Separating them decouples your API contract from your database schema, so you can change one without breaking the other, and it lets you expose only the fields a client should see rather than leaking the full table structure.",
  },
  {
    category: "spring",
    conceptId: "spring-dto-pattern",
    difficulty: "medium",
    question: "What concrete problems can leak if you return JPA entities directly from a controller in an app like TaskFlow?",
    answer: "You expose internal fields (audit columns, foreign keys, version numbers) and tie the JSON contract to the schema, so a column rename breaks clients. Lazy associations can also trigger LazyInitializationException during serialization because the persistence context is already closed, or cause unexpected extra queries. In TaskFlow you would map Task to a TaskResponse DTO in the service or controller so only intended fields like id, title, and status are returned.",
  },
  {
    category: "spring",
    conceptId: "spring-dto-pattern",
    difficulty: "hard",
    question: "Where should DTO-to-entity mapping live, and what are the trade-offs of manual mapping versus a library like MapStruct?",
    answer: "Mapping typically lives in the service layer (or a dedicated mapper) so controllers stay thin and entities never escape that boundary. Manual mapping is explicit, easy to debug, and has no runtime cost, but it is verbose and error-prone as objects grow. MapStruct generates the mapping code at compile time, so it is type-safe and fast with no reflection, at the cost of an annotation processor and slightly less obvious control flow than hand-written code.",
  },
  {
    category: "spring",
    conceptId: "spring-service-layer",
    difficulty: "easy",
    question: "What is the primary responsibility of the service layer in a Spring Boot application?",
    answer: "The service layer holds business logic and orchestrates work across repositories and other components, sitting between the controller and the data access layer. It is also the natural place to define transaction boundaries with @Transactional, so a single business operation either fully succeeds or fully rolls back. Controllers stay thin and repositories stay focused on persistence.",
  },
  {
    category: "spring",
    conceptId: "spring-service-layer",
    difficulty: "medium",
    question: "Why is the service layer the right place to put @Transactional rather than the controller or repository?",
    answer: "A business operation often spans multiple repository calls that must commit or roll back as a unit, and the service method is exactly that unit of work, so placing @Transactional there gives the correct boundary. Putting it on the controller pulls web concerns into the transaction and can keep it open during serialization, while putting it on the repository makes each call its own transaction and loses atomicity across multiple operations. In TaskFlow, a TaskService method that updates a task and writes an audit record should be one transaction.",
  },
  {
    category: "spring",
    conceptId: "spring-service-layer",
    difficulty: "hard",
    question: "In TaskFlow, a public service method calls another @Transactional method on the same bean via this. Why does the transaction not behave as expected?",
    answer: "Spring's @Transactional is implemented with a proxy, and self-invocation through this bypasses the proxy, so the inner method's transactional settings (like a new propagation level) are ignored. The call simply runs within whatever transaction the outer method already started, or with none if the outer method is not transactional. Fixes include splitting the logic into a separate bean so the call goes through the proxy, self-injecting the bean, or using AspectJ weaving instead of proxy-based AOP.",
  },
  {
    category: "spring",
    conceptId: "spring-layered-arch",
    difficulty: "easy",
    question: "Describe the Controller to Service to Repository layering and what each layer is responsible for.",
    answer: "The controller handles HTTP concerns: it receives requests, validates and binds input, and returns responses. The service contains business logic and transaction boundaries, coordinating one or more repositories. The repository handles data access, talking to the database. Each layer depends only on the one below it, which keeps responsibilities separated and the code easier to test and change.",
  },
  {
    category: "spring",
    conceptId: "spring-layered-arch",
    difficulty: "medium",
    question: "Why should the controller depend on a service interface rather than calling the repository directly in TaskFlow?",
    answer: "Skipping the service couples HTTP handling to persistence and leaves no clear home for business rules or transaction boundaries, so logic ends up scattered in controllers. Routing through TaskService keeps the controller thin, centralizes rules like authorization checks and validation of state transitions, and makes the logic unit-testable without a web context. It also means you can change the data access strategy without touching the controller.",
  },
  {
    category: "spring",
    conceptId: "spring-layered-arch",
    difficulty: "hard",
    question: "How does layered architecture relate to the dependency rule, and how do you keep entities or persistence concerns from leaking upward through the layers?",
    answer: "The dependency rule says each layer depends only downward (controller on service, service on repository), so higher layers never know about lower-layer details like SQL or HTTP. To stop leakage you map entities to DTOs at the service or controller boundary so JPA entities never reach the client, and you avoid returning repository-specific types from services. This keeps the web layer ignorant of persistence and lets you swap implementations behind interfaces without ripple effects.",
  },
  {
    category: "spring",
    conceptId: "spring-testing-unit",
    difficulty: "easy",
    question: "What do @Mock and @InjectMocks do in a Mockito unit test?",
    answer: "@Mock creates a mock instance of a dependency, such as a TaskRepository, whose behavior you stub instead of hitting a real database. @InjectMocks creates the class under test, like TaskService, and injects the declared mocks into it. Together they let you test the service in isolation, controlling exactly what its collaborators return.",
  },
  {
    category: "spring",
    conceptId: "spring-testing-unit",
    difficulty: "medium",
    question: "Why are Mockito unit tests for a service preferred over loading the full Spring context, and how do you enable Mockito annotations in JUnit 5?",
    answer: "Pure Mockito tests do not start the Spring container, so they run in milliseconds and isolate the class under test from the database, web layer, and configuration, making failures easy to localize. You stub collaborators with when().thenReturn() and verify interactions with verify(). In JUnit 5 you enable the annotations with @ExtendWith(MockitoExtension.class) on the test class, which initializes the @Mock and @InjectMocks fields.",
  },
  {
    category: "spring",
    conceptId: "spring-testing-unit",
    difficulty: "hard",
    question: "In a TaskService unit test, how do you verify that the service rejects an invalid state transition without saving, and what is the difference between stubbing and verifying?",
    answer: "You stub the repository to return the existing task, call the method expecting it to throw (using assertThrows), then assert no save happened with verify(taskRepository, never()).save(any()). Stubbing with when().thenReturn() sets up canned responses so the method under test can run, describing inputs, while verify() checks that an expected interaction did or did not occur, describing outputs. Mixing them up leads to either untested behavior or brittle over-specified tests.",
  },
  {
    category: "spring",
    conceptId: "spring-testing-integration",
    difficulty: "easy",
    question: "What does @SpringBootTest do, and how does it differ from a plain Mockito unit test?",
    answer: "@SpringBootTest bootstraps the full Spring application context for a test, wiring real beans together so you can verify components work in integration rather than in isolation. A Mockito unit test starts no context and replaces collaborators with mocks. Because it loads the context, @SpringBootTest is slower and is used to validate that the pieces fit together, not to test a single class's logic.",
  },
  {
    category: "spring",
    conceptId: "spring-testing-integration",
    difficulty: "medium",
    question: "What is MockMvc and why use it instead of making real HTTP calls when testing TaskController?",
    answer: "MockMvc lets you exercise the web layer by dispatching simulated requests through the Spring MVC machinery (routing, argument binding, validation, exception handling) without starting a real server or opening a socket. You can assert status codes, headers, and JSON body, for example verifying that POST /tasks with invalid input returns 400 via the global exception handler. It is faster and more deterministic than full network calls, and you can scope it tightly with @WebMvcTest to load just the web layer for that controller.",
  },
  {
    category: "spring",
    conceptId: "spring-testing-integration",
    difficulty: "hard",
    question: "How does @DataJpaTest differ from @SpringBootTest, and what does it configure by default for testing TaskRepository?",
    answer: "@DataJpaTest is a sliced test that loads only JPA-related beans (entities, repositories, EntityManager) instead of the whole context, so it is much faster and focused on persistence. By default it replaces any configured datasource with an in-memory embedded database and wraps each test in a transaction that rolls back afterward for isolation. This lets you verify derived queries and custom @Query methods on TaskRepository without the cost or side effects of a full @SpringBootTest, though you can opt into the real database with @AutoConfigureTestDatabase(replace = Replace.NONE).",
  },
  {
    category: "spring",
    conceptId: "spring-aop",
    difficulty: "medium",
    question: "What is AOP in Spring, and what problem does an @Around advice on an @Aspect solve?",
    answer: "AOP lets you modularize cross-cutting concerns like logging, security, or timing that would otherwise be scattered across many classes. An @Around advice wraps a target method via a ProceedingJoinPoint, so you can run code before and after the call and decide whether and when to invoke proceed(). In TaskFlow this is ideal for a logging or performance aspect around TaskService methods, keeping the service code focused purely on business logic while the timing concern lives in one place.",
  },
  {
    category: "spring",
    conceptId: "spring-aop",
    difficulty: "hard",
    question: "Why does a self-invocation of an @Transactional or @Aspect-advised method inside the same bean often fail to trigger the advice?",
    answer: "Spring AOP is proxy-based, so the advice only fires when the call goes through the proxy that wraps the bean. When a method calls another method on the same instance using this, the call bypasses the proxy entirely and hits the raw object, so no advice runs. The fix is to inject a reference to the bean's own proxy, split the logic into a second bean, or switch to compile-time/load-time weaving with AspectJ which weaves the advice directly into the bytecode.",
  },
  {
    category: "spring",
    conceptId: "spring-caching",
    difficulty: "easy",
    question: "What does @Cacheable do, and what must you add to a Spring Boot app to make it work?",
    answer: "@Cacheable stores a method's return value keyed by its arguments, so subsequent calls with the same arguments return the cached value without executing the method body. You must enable caching with @EnableCaching on a configuration class and provide a CacheManager (Spring Boot auto-configures a simple in-memory ConcurrentHashMap-based one, or you can back it with Caffeine or Redis). In TaskFlow you might cache a frequently read getTaskById lookup to avoid hitting the database on every request.",
  },
  {
    category: "spring",
    conceptId: "spring-caching",
    difficulty: "medium",
    question: "In TaskFlow, if you cache getTaskById, how do you keep the cache from going stale when a task is updated or deleted?",
    answer: "You pair the read with eviction so a write invalidates the corresponding entry. Annotate updateTask and deleteTask with @CacheEvict on the same cache and the same key, for example @CacheEvict(value=\"tasks\", key=\"#id\"), so the next read repopulates fresh data. For bulk changes you can use @CacheEvict(allEntries=true), and @CachePut lets an update method always run while refreshing the cache with the new return value.",
  },
  {
    category: "spring",
    conceptId: "spring-async",
    difficulty: "medium",
    question: "How do @Async and @EnableAsync work together, and what should an @Async method return?",
    answer: "@EnableAsync activates async processing, and @Async makes a method run on a separate thread from a task executor instead of the caller's thread. The call returns immediately, so the method should return void or a CompletableFuture (or Future) so the caller can later retrieve the result or handle errors. In TaskFlow this fits sending a notification or email after task creation without blocking the HTTP response.",
  },
  {
    category: "spring",
    conceptId: "spring-async",
    difficulty: "hard",
    question: "Why is relying on the default executor for @Async risky in production, and how do you control the thread pool?",
    answer: "If no executor is found, Spring falls back to a SimpleAsyncTaskExecutor, which spawns a new thread per task with no pool limit and can exhaust resources under load (note that Spring Boot's auto-configured applicationTaskExecutor is a bounded ThreadPoolTaskExecutor, but it is easy to bypass). You should define a ThreadPoolTaskExecutor bean with explicit core size, max size, and queue capacity, and reference it by name in @Async(\"executorName\"). You also set a RejectedExecutionHandler and an AsyncUncaughtExceptionHandler, because exceptions thrown from void @Async methods are otherwise swallowed rather than propagated to the caller.",
  },
  {
    category: "spring",
    conceptId: "spring-actuator",
    difficulty: "easy",
    question: "What is Spring Boot Actuator, and what does the /actuator/health endpoint tell you?",
    answer: "Actuator adds production-ready endpoints for monitoring and managing a running application, exposed over HTTP under /actuator. The health endpoint aggregates health indicators, such as database and disk space, into an overall UP or DOWN status that load balancers and orchestrators like Kubernetes use for liveness and readiness probes. For TaskFlow, a DOWN status would surface, for example, that the database connection has failed.",
  },
  {
    category: "spring",
    conceptId: "spring-actuator",
    difficulty: "medium",
    question: "Actuator endpoints can leak sensitive data. How do you expose and secure them safely in Spring Boot 3.x?",
    answer: "By default only health is exposed over HTTP; you opt in to others via management.endpoints.web.exposure.include, so you expose only what you need rather than everything. Endpoints like env, heapdump, and beans reveal sensitive internals, so you secure the /actuator/** path with Spring Security, often requiring an admin role. A common pattern is to move Actuator to a separate management port via management.server.port and restrict it at the network level so it is never publicly reachable.",
  },
  {
    category: "spring",
    conceptId: "spring-profiles",
    difficulty: "easy",
    question: "What is a Spring profile, and how does application-dev.yml relate to it?",
    answer: "A profile is a named group of beans and configuration that is only active in certain environments, letting one codebase behave differently in dev, test, and prod. A file named application-dev.yml is loaded and merged on top of application.yml only when the dev profile is active, set via spring.profiles.active. In TaskFlow this lets you point at an in-memory H2 database in dev and a real Postgres instance in prod without changing code.",
  },
  {
    category: "spring",
    conceptId: "spring-profiles",
    difficulty: "medium",
    question: "How does @Profile control bean creation, and why is that useful for something like TaskFlow's security or mail setup?",
    answer: "@Profile on a bean or configuration class makes that bean eligible for registration only when the listed profile is active, otherwise it is skipped entirely. This lets you swap whole implementations by environment, for example a no-op or console-logging email sender under @Profile(\"dev\") and a real SMTP sender under @Profile(\"prod\"). It is cleaner than runtime if-checks because the unused bean is never created, and you can even relax security or enable verbose logging only in non-production profiles.",
  },
  {
    category: "spring",
    conceptId: "spring-event-driven",
    difficulty: "medium",
    question: "How does Spring's ApplicationEvent and @EventListener mechanism help decouple code in an app like TaskFlow?",
    answer: "A component publishes an event through ApplicationEventPublisher, and any bean with an @EventListener method for that event type reacts, without the publisher knowing who listens. This decouples the producer from consumers, so TaskService can publish a TaskCreatedEvent and separate listeners can send a notification, write an audit log, or update a cache independently. Adding new reactions means adding new listeners, not editing the service, which keeps the core logic clean.",
  },
  {
    category: "spring",
    conceptId: "spring-event-driven",
    difficulty: "hard",
    question: "Why are default Spring events synchronous, and how do @TransactionalEventListener and @Async change the behavior?",
    answer: "By default @EventListener runs in the same thread as the publisher and, when the publisher is transactional, within that same transaction, so a slow or failing listener can delay the publisher or cause a rollback. @Async on the listener moves it to a separate thread so the publisher does not wait, but then it no longer participates in the publisher's transaction. @TransactionalEventListener instead binds the listener to a transaction phase such as AFTER_COMMIT, which is the safe way to, for example, send a TaskFlow notification only once the task is actually committed to the database.",
  },
  {
    category: "spring",
    conceptId: "spring-bean-scopes",
    difficulty: "easy",
    question: "What is the difference between singleton and prototype bean scopes in Spring?",
    answer: "Singleton, the default, means the container creates exactly one shared instance per application context and hands the same object to every injection point. Prototype means a new instance is created each time the bean is requested or injected. Singletons are great for stateless services like TaskService, while prototype suits beans that hold per-use mutable state you do not want shared.",
  },
  {
    category: "spring",
    conceptId: "spring-bean-scopes",
    difficulty: "hard",
    question: "If you inject a prototype-scoped or request-scoped bean into a singleton, why might it not behave as expected, and how do you fix it?",
    answer: "The singleton resolves its dependency once at creation time, so it holds onto a single instance forever and never gets a fresh prototype or a per-request bean as you intended. The fix is to break the lifecycle mismatch using a scoped proxy via proxyMode on the scope annotation, or to look up the bean lazily through ObjectProvider or a provider method. The proxy is injected into the singleton but transparently delegates each call to the correct short-lived instance, which is exactly how request scope works in a web app like TaskFlow.",
  },
  {
    category: "spring",
    conceptId: "spring-circular-deps",
    difficulty: "medium",
    question: "What is a circular dependency in Spring, and why does constructor injection expose it more readily than field injection?",
    answer: "A circular dependency is when bean A needs bean B and bean B needs bean A, so neither can be fully constructed first. With constructor injection both beans require the other to already exist before they can be instantiated, so Spring cannot resolve the order and fails fast at startup with a clear error. Field or setter injection can sometimes hide the cycle because the bean is constructed first and the dependency is set afterward, which is why surfacing it early with constructor injection is actually a benefit, not a flaw.",
  },
  {
    category: "spring",
    conceptId: "spring-circular-deps",
    difficulty: "hard",
    question: "A circular dependency between two TaskFlow services usually signals a design problem. What are the real fixes versus the quick hacks?",
    answer: "The proper fix is to redesign: extract the shared logic into a third collaborator, or publish an ApplicationEvent so one service reacts to the other instead of calling it directly, which removes the cycle entirely. Quick hacks include @Lazy on one dependency so its proxy is injected and resolved only on first use, or setter injection to defer the wiring. Note that Spring Boot 2.6+ disables circular references by default, so relying on the container to auto-resolve them now requires explicitly setting spring.main.allow-circular-references to true, which is discouraged.",
  },
  {
    category: "dsa",
    conceptId: "arrays-hashing",
    difficulty: "easy",
    question: "When you see a problem asking 'have I seen this value before?', 'count occurrences', or 'find duplicates', which pattern should you reach for and why?",
    answer: "Reach for a hash map or hash set. The core insight is trading space for time: a hash structure gives average O(1) lookups, so you can check membership or counts in one pass instead of an O(n) nested scan. This turns many naive O(n^2) brute-force solutions into O(n) time at the cost of O(n) extra space.",
  },
  {
    category: "dsa",
    conceptId: "arrays-hashing",
    difficulty: "medium",
    question: "Two strings are anagrams. What is the standard hashing approach and what is its complexity compared to sorting?",
    answer: "Build a frequency map (or a fixed 26-length count array for lowercase letters) of one string, then decrement while scanning the other; all counts ending at zero means they are anagrams. With a bounded alphabet this runs in O(n) time and O(1) extra space, beating the O(n log n) sort-and-compare approach. The why: comparing character frequencies is order-independent, which is exactly what 'anagram' means.",
  },
  {
    category: "dsa",
    conceptId: "two-pointers",
    difficulty: "easy",
    question: "What property of the input typically signals that the two-pointers technique applies, and what is its key advantage?",
    answer: "A sorted array (or one you can sort) where you are searching for a pair or triplet meeting a sum or comparison condition is the classic signal, as is any task processing a sequence from both ends inward. The advantage is reducing an O(n^2) pair search to O(n) by moving pointers based on whether the current sum is too small or too large, never re-examining discarded elements because sorting guarantees the direction of correction.",
  },
  {
    category: "dsa",
    conceptId: "two-pointers",
    difficulty: "medium",
    question: "In the container-with-most-water style problem, why is it always safe to move the pointer at the shorter line inward rather than the taller one?",
    answer: "Area is width times the shorter of the two heights, so the height is bottlenecked by the smaller line. Moving the taller pointer can only shrink the width while the limiting shorter height stays the same or smaller, so it can never improve the answer. Moving the shorter pointer is the only move that could find a taller bottleneck, which makes the greedy two-pointer sweep correct in O(n).",
  },
  {
    category: "dsa",
    conceptId: "sliding-window",
    difficulty: "easy",
    question: "What kind of problem phrasing tells you to use a sliding window, and how does it differ from a fixed nested-loop scan?",
    answer: "Problems asking for the longest, shortest, or some optimum over a contiguous subarray or substring satisfying a constraint are the signal. Instead of recomputing every subarray in O(n^2), you maintain a window with two pointers and incrementally add the entering element and remove the leaving one, achieving O(n). The key is that the window's running state (sum, count, char frequencies) can be updated in O(1) per step rather than recomputed.",
  },
  {
    category: "dsa",
    conceptId: "sliding-window",
    difficulty: "hard",
    question: "What is the difference between a fixed-size and a variable-size sliding window, and what is the common pitfall when shrinking a variable window?",
    answer: "A fixed-size window slides by one each step keeping length k constant, while a variable-size window expands the right pointer to grow and advances the left pointer to shrink until a constraint is restored. The common pitfall is shrinking with an if instead of a while: when the constraint can be violated by more than one unit, a single left-advance is insufficient, so you must loop the left pointer until the window is valid again. Forgetting to update the window state (e.g. decrement a frequency) as left moves is the other frequent bug.",
  },
  {
    category: "dsa",
    conceptId: "stack-basics",
    difficulty: "medium",
    question: "What is a monotonic stack, and what class of problems is it the right tool for?",
    answer: "A monotonic stack keeps its elements in strictly increasing or decreasing order by popping any element that violates the order before pushing a new one. It is the right tool for 'next/previous greater or smaller element' problems and many histogram or span problems. The why: each element is pushed and popped at most once, so you resolve all such relationships in O(n) total instead of O(n^2) pairwise comparisons.",
  },
  {
    category: "dsa",
    conceptId: "stack-basics",
    difficulty: "easy",
    question: "Why is a stack the natural data structure for validating balanced parentheses or brackets?",
    answer: "Brackets must close in last-opened-first-closed order, which is exactly LIFO behavior. You push each opening bracket and, on a closing bracket, pop and verify it matches the expected pair; a mismatch or empty stack means invalid. The string is valid only if the stack is empty at the end, all in O(n) time and O(n) space.",
  },
  {
    category: "dsa",
    conceptId: "binary-search",
    difficulty: "medium",
    question: "Binary search obviously applies to a sorted array, but what is the more general 'binary search on the answer' technique and when does it apply?",
    answer: "Binary search on the answer applies when the candidate answers form a monotonic predicate: there is a threshold below which a check returns false and at or above which it returns true (or vice versa). You binary search over the value range, calling a feasibility check at each midpoint, and converge on the boundary. It applies to problems like minimum capacity, minimum speed, or smallest divisor, turning an O(range) linear scan into O(log range) times the check cost.",
  },
  {
    category: "dsa",
    conceptId: "binary-search",
    difficulty: "hard",
    question: "What are the two most common bugs in binary search implementations and how do you avoid them?",
    answer: "The first is integer overflow when computing the midpoint as (low + high) / 2 on large bounds; compute it as low + (high - low) / 2 instead. The second is an incorrect loop boundary or update that causes an infinite loop or off-by-one, especially in lower-bound or upper-bound variants. The fix is to commit to one invariant, such as whether high is inclusive or exclusive, and to ensure every iteration strictly shrinks the search space so termination is guaranteed.",
  },
  {
    category: "dsa",
    conceptId: "linked-list",
    difficulty: "medium",
    question: "What is the fast-and-slow pointer (tortoise and hare) technique, and what two classic problems does it solve in O(1) space?",
    answer: "You advance one pointer one node per step and another two nodes per step. It detects a cycle, because in a loop the fast pointer eventually laps and meets the slow one, and it finds the middle of a list, because when fast reaches the end the slow pointer is at the midpoint. Both run in O(n) time with O(1) extra space, avoiding the O(n) space of storing visited nodes in a set.",
  },
  {
    category: "dsa",
    conceptId: "linked-list",
    difficulty: "easy",
    question: "When reversing a singly linked list iteratively, why do you need to capture the next node before re-pointing, and what is the role of a dummy head node generally?",
    answer: "Re-pointing current.next to the previous node destroys the original forward link, so you must save current.next first or you lose the rest of the list. A dummy (sentinel) head node, used in many list problems, removes special-casing for the head: insertions, deletions, and merges can treat the real first node uniformly, simplifying code and avoiding null-head edge cases.",
  },
  {
    category: "dsa",
    conceptId: "trees-dfs",
    difficulty: "medium",
    question: "When should you reach for DFS on a tree rather than BFS, and what is the relationship between recursive DFS and the call stack?",
    answer: "Reach for DFS when the problem is about root-to-leaf paths, subtree properties, or anything naturally expressed as 'solve the children, then combine for the parent' such as height, path sums, or validating a BST. Recursive DFS uses the program's call stack implicitly, so its space cost is O(h) where h is the tree height, which is O(n) for a degenerate skewed tree and O(log n) for a balanced one. An explicit stack achieves the same iteratively when recursion depth is a concern.",
  },
  {
    category: "dsa",
    conceptId: "trees-dfs",
    difficulty: "hard",
    question: "What is the difference between preorder, inorder, and postorder DFS, and why is inorder special for binary search trees?",
    answer: "The three differ in when you process the current node relative to its children: preorder processes node before children, inorder processes left subtree then node then right, and postorder processes children before the node. Inorder is special for a BST because visiting left, node, right yields the values in sorted ascending order, which makes inorder the go-to for validating a BST or finding the kth smallest element. Postorder is the natural choice when a node's result depends on fully computed child results, such as deleting a tree or computing subtree sums.",
  },
  {
    category: "dsa",
    conceptId: "trees-bfs",
    difficulty: "easy",
    question: "What signals that a tree problem needs BFS (level-order traversal) instead of DFS, and what data structure drives it?",
    answer: "Problems asking for anything per level, the shortest path or minimum depth, or a level-by-level grouping signal BFS. It is driven by a queue (FIFO): you enqueue the root, then repeatedly dequeue a node and enqueue its children, processing nodes in order of increasing depth. Tracking the queue size at the start of each round lets you cleanly separate one level from the next.",
  },
  {
    category: "dsa",
    conceptId: "trees-bfs",
    difficulty: "medium",
    question: "In level-order traversal, how do you capture each level as a separate group, and what is the worst-case space complexity of tree BFS?",
    answer: "At the start of each iteration, record the current queue size n; that count is exactly the number of nodes on the current level, so you dequeue exactly n nodes into one level's list before moving on. The worst-case space is O(w) where w is the maximum width of the tree, which for a balanced binary tree is roughly n/2 nodes at the bottom level, making it O(n). This is why BFS can use more memory than DFS on wide, balanced trees.",
  },
  {
    category: "dsa",
    conceptId: "backtracking",
    difficulty: "medium",
    question: "What is the backtracking template, and what kinds of problems is it the right pattern for?",
    answer: "Backtracking is a controlled DFS over a decision tree: you make a choice, recurse, then undo the choice (backtrack) to explore alternatives. It is the right pattern for generating all subsets, permutations, combinations, or for constraint-satisfaction problems like N-Queens, Sudoku, and word search. The why: it systematically explores every valid configuration while pruning invalid branches early, which avoids enumerating the full exponential space when constraints let you abandon paths.",
  },
  {
    category: "dsa",
    conceptId: "backtracking",
    difficulty: "hard",
    question: "Why is backtracking typically exponential in time, and how do pruning and handling duplicates improve it?",
    answer: "Backtracking explores a decision tree whose size grows exponentially: subsets are O(2^n) and permutations O(n!), and each leaf costs O(n) to copy, so the cost is inherently large. Pruning helps by cutting branches that cannot lead to a valid or better solution as early as possible, such as abandoning a partial sum that already exceeds the target. To avoid duplicate results you sort the input and skip a candidate equal to its predecessor at the same recursion depth, ensuring each distinct combination is generated only once.",
  },
  {
    category: "dsa",
    conceptId: "heap-priority",
    difficulty: "medium",
    question: "What problem signals point to a heap (priority queue), and why is it better than sorting for a 'top K' query?",
    answer: "Phrases like 'kth largest/smallest', 'top K frequent', or 'merge K sorted lists', and any need to repeatedly pull the current min or max from a changing set, point to a heap. For top K, a bounded heap of size k gives O(n log k) time versus O(n log n) for a full sort, and it uses only O(k) space. The why: you never need the full ordering, only the k extreme elements, so maintaining a small heap and evicting the worst is cheaper than sorting everything.",
  },
  {
    category: "dsa",
    conceptId: "heap-priority",
    difficulty: "hard",
    question: "To find the kth largest element with a heap, should you use a min-heap or a max-heap of which size, and what is the complexity?",
    answer: "Use a min-heap of size k: push elements and whenever the heap exceeds k, pop the smallest, so the heap always holds the k largest seen and its root is the kth largest. This is O(n log k) time and O(k) space, which beats building a max-heap of all n elements (O(n)) and popping k times (O(k log n)). The trick is that the min-heap's root is the smallest of the k largest, which is precisely the kth largest overall.",
  },
  {
    category: "dsa",
    conceptId: "dp-1d",
    difficulty: "medium",
    question: "How do you recognize that a problem is 1D dynamic programming, and what two ingredients must you identify?",
    answer: "Suspect 1D DP when a problem asks for an optimum (max, min, count, or feasibility) over choices where the answer at position i depends on a small number of earlier positions, and where naive recursion recomputes the same subproblems. You must identify the state, typically dp[i] meaning the best answer ending at or up to index i, and the recurrence relating dp[i] to earlier states such as dp[i-1] and dp[i-2]. Confirming optimal substructure and overlapping subproblems is what distinguishes DP from greedy.",
  },
  {
    category: "dsa",
    conceptId: "dp-1d",
    difficulty: "hard",
    question: "In house-robber style 1D DP, why can the rolling array reduce space to O(1), and what is the recurrence?",
    answer: "The recurrence is dp[i] = max(dp[i-1], dp[i-2] + value[i]): at each house you either skip it and keep the best so far, or rob it and add it to the best two houses back. Because dp[i] only ever references dp[i-1] and dp[i-2], you do not need the whole array; two scalar variables tracking those two values suffice, collapsing O(n) space to O(1) while keeping O(n) time. This rolling-variable optimization applies whenever a state depends only on a constant number of previous states.",
  },
  {
    category: "dsa",
    conceptId: "dp-2d",
    difficulty: "medium",
    question: "What signals a 2D DP problem, and how do you define the state for grid-path or two-sequence problems?",
    answer: "Two interacting dimensions signal 2D DP: a grid where you move between cells, or two strings or arrays being compared such as in edit distance or longest common subsequence. For grid paths the state dp[i][j] is the best way to reach cell (i, j), typically from the top or left; for two sequences dp[i][j] is the answer for the first i characters of one and first j of the other. The recurrence combines adjacent cells, and the whole table fills in O(rows times cols) time.",
  },
  {
    category: "dsa",
    conceptId: "dp-2d",
    difficulty: "hard",
    question: "In longest common subsequence, what does dp[i][j] mean, what is the recurrence, and how can space be reduced?",
    answer: "dp[i][j] is the length of the LCS of the first i characters of string A and the first j of string B. If the characters match, dp[i][j] = dp[i-1][j-1] + 1; otherwise dp[i][j] = max(dp[i-1][j], dp[i][j-1]). Since each row depends only on the previous row, you can reduce the full O(m times n) table to two rows; iterating with the shorter string as the row length makes the space O(min(m, n)) while keeping O(m times n) time.",
  },
  {
    category: "dsa",
    conceptId: "graphs-bfs-dfs",
    difficulty: "medium",
    question: "On an unweighted graph, when do you choose BFS over DFS, and why does BFS give shortest paths while DFS does not?",
    answer: "Choose BFS when you need the shortest path or minimum number of edges between nodes in an unweighted graph; choose DFS for connectivity, cycle detection, or exhaustive path exploration. BFS explores nodes in order of increasing distance from the source using a queue, so the first time it reaches a node it has done so via the fewest edges. DFS dives deep along one branch first, so it may reach a node by a longer route before a shorter one, which is why it does not guarantee shortest paths.",
  },
  {
    category: "dsa",
    conceptId: "graphs-bfs-dfs",
    difficulty: "hard",
    question: "Why is a visited set essential in graph traversal, and what is the time complexity of BFS or DFS on an adjacency-list graph?",
    answer: "Unlike trees, graphs can have cycles and multiple paths to the same node, so without a visited set you would revisit nodes endlessly or do redundant work, risking infinite loops. Marking a node visited when you first enqueue or enter it ensures each node and edge is processed once. With an adjacency list this gives O(V + E) time, where V is vertices and E is edges, plus O(V) space for the visited set and the queue or recursion stack.",
  },
  {
    category: "dsa",
    conceptId: "intervals",
    difficulty: "medium",
    question: "What is the standard first step for almost every interval problem, and why does it make merging correct?",
    answer: "Sort the intervals by their start value (sometimes by end, for greedy scheduling). Once sorted, any interval that overlaps the current merged interval must start before the current one ends, so you only ever need to compare each interval to the last merged result rather than all previous ones. This makes a single linear pass after sorting sufficient, giving O(n log n) total time dominated by the sort.",
  },
  {
    category: "dsa",
    conceptId: "intervals",
    difficulty: "hard",
    question: "How do you decide whether two intervals overlap, and what is the classic mistake when merging or counting overlaps?",
    answer: "After sorting by start, two intervals overlap when the next interval's start is less than or equal to the current interval's end; when they merge, the new end is the maximum of the two ends, not simply the second interval's end. The classic mistake is taking the later interval's end blindly, which incorrectly shrinks the merged range when the earlier interval extends further (for example merging [1,10] with [2,3] must keep end at 10). Forgetting the equality case at boundaries, depending on whether touching intervals count as overlapping, is the other frequent error.",
  },
  {
    category: "behavioral",
    conceptId: null,
    difficulty: "medium",
    question: "Tell me about a time you had to learn something complex quickly.",
    answer: "Use STAR with the TaskFlow transaction work. Situation: concurrent task updates were overwriting each other and I had only shallow knowledge of Spring's transaction model. Task: I had to make updates safe under concurrency on a tight timeline. Action: I dug into propagation, isolation levels, and optimistic locking via a @Version column, validating my understanding with focused tests rather than guessing. Result: updates became atomic and safe, and I could explain exactly how Spring manages transaction boundaries. Keep it grounded in one concrete decision and emphasize how you verified your new knowledge.",
  },
  {
    category: "behavioral",
    conceptId: null,
    difficulty: "medium",
    question: "Describe a tricky bug you tracked down and how you debugged it.",
    answer: "Anchor it to the N+1 query bug in TaskFlow. Situation: listing tasks was slow in production-like data volumes. Task: find why a simple findAll triggered a flood of queries. Action: I enabled SQL logging, saw one query per task for a lazy association, and confirmed the N+1 pattern; I fixed it with an @EntityGraph fetch join. Result: query count dropped from N+1 to one and latency fell sharply. Show your method, observe the symptom, form a hypothesis, confirm with evidence, fix the root cause, not just the surface.",
  },
  {
    category: "behavioral",
    conceptId: null,
    difficulty: "medium",
    question: "Tell me about a time you disagreed with a teammate or a technical decision.",
    answer: "Use a TaskFlow design choice such as returning entities versus DTOs from controllers. Situation: there was a push to return JPA entities directly to keep things simple. Task: I believed that leaked the persistence model and risked lazy-loading and serialization issues. Action: I proposed a DTO layer, showed a small example of the coupling problem, and listened to the maintenance-cost concern. Result: we agreed on DTOs for external responses. Emphasize that you disagreed respectfully, argued with evidence, and stayed open to the other view.",
  },
  {
    category: "behavioral",
    conceptId: null,
    difficulty: "medium",
    question: "Tell me about a project you're proud of.",
    answer: "Pick TaskFlow and frame the whole thing in STAR. Situation: I wanted a real backend that exercised production concerns, not a toy CRUD app. Task: build a secure, well-layered task-management API. Action: I designed a clean Controller to Service to Repository structure, added JWT authentication via a filter, centralized errors in a @ControllerAdvice handler, and made updates concurrency-safe with @Transactional. Result: a coherent system I can reason about end to end. Be specific about one or two hard problems you solved so it reads as genuine ownership, not a feature list.",
  },
  {
    category: "behavioral",
    conceptId: null,
    difficulty: "medium",
    question: "Describe a tradeoff you had to make in a design or implementation.",
    answer: "Use the locking strategy in TaskFlow. Situation: concurrent updates needed protection. Task: choose between pessimistic and optimistic locking. Action: I chose optimistic locking with a @Version column because conflicts were rare and I wanted to avoid holding database locks and hurting throughput, accepting that occasional conflicts would surface as retryable failures. Result: good concurrency under normal load with safe behavior on the rare clash. Make the tradeoff explicit, state what you gave up and why the gain was worth it for this context.",
  },
  {
    category: "behavioral",
    conceptId: null,
    difficulty: "medium",
    question: "Tell me about a time you improved the performance of a system.",
    answer: "Center it on the TaskFlow N+1 fix and lay it out in STAR. Situation: the task list endpoint issued one extra query per task because of a lazily loaded association. Task: cut the query count without breaking the lazy default elsewhere. Action: I measured with SQL logging, then used an @EntityGraph on the specific repository method to fetch the association in a single join. Result: queries dropped from N+1 to one and response time improved measurably. Lead with measurement before and after so the improvement is concrete, not anecdotal.",
  },
  {
    category: "behavioral",
    conceptId: null,
    difficulty: "medium",
    question: "Tell me about a time you had to deliver under a tight deadline.",
    answer: "Use the JWT security work in TaskFlow. Situation: the API needed authentication before it could be shared, with little time. Task: secure the endpoints without over-engineering. Action: I scoped to the essentials, a SecurityFilterChain plus a OncePerRequestFilter to validate the token and set the SecurityContext, and deferred refresh tokens and role hierarchies to later. Result: endpoints were protected on time and the design left room to extend. Show that you protected scope, prioritized the critical path, and were deliberate about what to defer rather than cutting corners blindly.",
  },
  {
    category: "behavioral",
    conceptId: null,
    difficulty: "medium",
    question: "Tell me about a mistake you made and how you handled it.",
    answer: "Pick an honest one from TaskFlow, for example initially catching exceptions ad hoc in controllers. Situation: error responses were inconsistent and some leaked stack traces. Task: I needed uniform, safe error handling once I noticed the mess. Action: I owned it, consolidated handling into a @ControllerAdvice with @ExceptionHandler methods returning ProblemDetail, and added tests for the error paths. Result: consistent, client-friendly responses and no leaked internals. The key is owning the mistake plainly, explaining what you learned, and showing the concrete fix you put in place.",
  },
];
